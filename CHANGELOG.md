# 변경 이력

## 3.2.2 - 2026-09-24

- 예스24 API의 상품 분류를 장르로 저장합니다. 공개 상품 페이지에 태그가 있는 경우 선택한 상품을 적용할 때 해당 태그를 가져옵니다.
- 예스24 판타지/무협·로맨스·BL 웹소설 계열에는 소장용 전자책이어도 `웹소설` 장르를 추가합니다. 라이트노벨과 일반 도서에는 이 장르를 추가하지 않습니다.
- 예스24 장르가 없는 이전 자동 검색 후보를 재사용하지 않도록 검색 캐시를 갱신했습니다.

## 3.2.1 - 2026-09-24

- 예스24 검색 결과를 자료 유형별로 구분해 다른 유형의 상품이 수동·자동 메타데이터 후보에 섞이지 않도록 했습니다.
- 예스24 판타지/무협·로맨스·라이트노벨과 하위 분류를 소설로 인식합니다. `환생자의 재벌테크`처럼 번호 없는 전자책도 소설 후보로 찾습니다.
- 예스24의 `화`·`[연재]` 상품은 연재판, `권`·번호 없는 상품은 일반 전자책으로 구분합니다. 수동 검색에서는 두 판본을 각각 표시하고, 자동 수집에서는 파일명에 맞는 판본을 고릅니다.

## 3.2.0 - 2026-09-24

- 예스24 공식 Open API를 수동·자동 메타데이터 검색 제공처로 추가했습니다. 플러그인 설정에서 API 키와 검색 우선순위를 지정할 수 있습니다.
- 예스24 API에 작품 소개가 없으면 공개 상품 페이지의 소개를 가져오고, 작가명에 붙은 `저`를 제거하며, 출간일을 날짜 형식으로 저장합니다. 저장된 표지가 없는 PDF는 허용된 자료 유형일 때 외부 표지를 사용할 수 있습니다.
- 리디·교보 검색 후보에 해당 상품의 표지를 표시하도록 보완했습니다. 교보 책 소개에 포함되던 `펼치기` 버튼 문구도 제외합니다.
- 상세페이지의 추가 정보 요청 제한 시간을 20초에서 60초로 늘렸습니다.

## 3.1.1 - 2026-09-24

- 기존 연재 시리즈에 `(완결)` 표시가 있는 새 파일이 들어오면 리디 등 설정된 제공처를 새로 조회합니다. 제공처에서도 완결이 확인된 경우에만 연재상태·연재종료일·새 태그를 갱신하고, 기존 태그와 다른 메타데이터는 유지합니다. 제공처에 종료일이 없으면 날짜를 추정하지 않습니다.
- 리디 소설 검색에서 판타지·로맨스·로판·무협 e북을 포함하고 만화 e북은 제외합니다. `전설의 기사 아크리안`처럼 판타지 e북으로 분류된 작품도 찾을 수 있습니다.
- 리디 웹소설은 장르에 `웹소설`을 추가하고, 소설 e북은 `소설 e북`으로 표시합니다. 전체 제목을 수동 검색할 때 제목이 짧은 다른 작품이 후보에 섞이던 문제도 수정했습니다.

## 3.1.0 - 2026-09-24

### 메타데이터 자동 수집과 적용

- 기존 값 덮어쓰기를 켜면 Kavita.yaml·ComicInfo.xml에서 먼저 채워진 메타데이터도 선택한 외부 값으로 갱신합니다. 상세 화면을 다시 열 때는 저장된 DB 값을 우선 표시합니다.
- 동명 작품의 작가가 달라 자동 적용을 보류하면 관리자 상세 화면에 제공처별 후보 제목·작가와 수동 검색 버튼을 표시합니다. 수동 적용 후에는 보류 안내를 지웁니다.
- 리디 상세 화면의 `외 N명` 축약 작가명 대신 검색 결과에 있는 전체 작가명을 확인해, 공통 작가가 있는 동일 작품의 자동 매칭을 보완했습니다.
- 장르 변환 규칙을 기존 저장 장르에도 적용해 `19+ → 성인`처럼 변환한 값과 원래 값이 함께 남지 않도록 했습니다. 제공처별 링크와 상충하는 판본 장르도 정리합니다.

### 표지와 화면 갱신

- 리디의 현재 상세 페이지에서 권별 표지를 읽고 각 권에 맞는 표지만 저장합니다. 1권만 있는 자료와 권 표기가 없는 단일 파일도 해당 표지를 사용할 수 있게 했습니다.
- 원격 Lazy Scanner가 나중에 표지를 바꾸더라도, 기존 표지 덮어쓰기 설정으로 선택한 외부 표지를 복원합니다.
- 메타데이터 적용이나 표지 변경 뒤 시리즈 목록을 갱신해 새로고침 없이 결과를 확인할 수 있게 했습니다.

### 관련작품

- Series.db 관계 항목에 한국어 제목이 없을 때 현재 작품명을 대신 사용하던 오연결을 제거했습니다. 사전 연재판처럼 원제가 같은 별도 항목이 현재 작품 자체로 표시되지 않도록 했습니다.
- Series.db의 `alternative` 관계 표시 이름을 `리메이크`에서 `다른 판본`으로 바꿨습니다.

## 3.0.2 - 2026-09-23

- 네이버웹툰을 웹툰 유형의 수동·자동 검색 제공처로 추가하고 연재·휴재·완결 상태, 연재 시작일, 완결일과 완결 회차 수를 수집하도록 했습니다.
- 네이버시리즈와 네이버웹툰을 고정 우선순위 묶음으로 처리하고, 파일명의 `화`·`권` 표기에 따라 연재본과 단행본을 선택하며 네이버웹툰의 연재 정보를 네이버시리즈 결과에 보완합니다.
- 네이버와 카카오 제공처 묶음은 각각 하나의 손잡이로 함께 이동하며, 위아래 이동 위치가 정확히 반영되도록 수정했습니다. 카카오 웹툰 유형에서는 제목과 작가가 일치하는 결과의 정보와 링크를 하나로 병합합니다.
- 네이버시리즈 검색 결과의 출판사를 `네이버시리즈`로 통일했습니다.
- 소설 라이브러리의 수동 검색 결과가 제공처의 `e북`·`단행본` 문구 때문에 `만화 e북`으로 표시되지 않도록, BL 소설 e북·웹소설·라이트노벨 유형을 공통 기준으로 표시합니다.

## 3.0.1 - 2026-09-23

- 카카오페이지의 `웹소설`과 세부 분류를 장르로 저장하고, 작품 정보의 테마 키워드만 태그로 저장하도록 수정했습니다.
- 카카오페이지의 연재 상태, 연재 시작일, 완결일, 전체 회차 수와 실제 출판사를 수집하도록 보완했습니다.
- 메타데이터 검색 중에는 빈 결과 안내를 숨기고 검색 진행 상태만 표시하도록 수정했습니다.
- 문피아 상세정보에서 연재 상태·시작일·완결일·회차 수·ISBN/Web ID를 수집하고, 문피아·노벨피아 장르에 `웹소설`을 추가했습니다.

## 3.0.0 - 2026-09-23

### 메타데이터 검색과 자동 매칭

- 카카오웹툰·카카오페이지·문피아·노벨피아를 수동/자동 검색과 제공처 우선순위 설정에 추가했습니다. 카카오웹툰은 웹툰 유형, 나머지는 소설 유형에 사용합니다.
- 폴더명의 `시리즈명 [작가명]`을 검증하고, 자동 검색은 제목 일치·수동 검색은 부분 제목을 지원합니다. 동명 작품의 작가가 충돌하면 자동 적용을 보류합니다.
- 노벨피아 비로그인 검색 누락을 보완하는 공개 검색과 작가 작품 목록 조회를 추가했습니다. 공개 검색에서도 찾지 못한 작품의 발견을 보장하지는 않습니다.
- 교보의 전체 책소개·저자·출판사·장르·ISBN·출간일과 리디의 연령등급·완결·출간일 수집을 보완했습니다. 제공처에 없는 날짜는 임의로 채우지 않습니다.
- 노벨피아 장르와 태그를 구분하고, 연재중단 및 완결 회차 정보를 반영합니다. 작품소개에 중복된 제목을 정리합니다.

### 표지 수집

- 내부 표지가 있는 자료는 외부 표지 수집에서 제외합니다.
- 웹툰은 내부 표지 유무와 관계없이 Series.db를 우선하고, 없으면 외부 메인 표지를 사용합니다. 모든 회차가 저장 파일 하나를 공유하며, 잠긴 자료는 기존 정책을 따릅니다.

### 상세정보와 편집

- 연령등급·회차 수 편집을 추가하고, 표시 등급과 편집창의 선택값을 일치시켰습니다.
- 도서 유형은 연재정보 대신 출간일을 표시하고 편집할 수 있습니다.
- 완결·연재중단 작품은 수집한 회차 수를 표시합니다. 연재 중에는 수동 입력값만 표시하며, 수동 값이 있으면 우선합니다.
- 완결 작품의 총회차와 TXT/EPUB 파일명 회차 범위를 비교해 누락을 판정합니다.

### 홈 위젯과 설정

- 신규 도서 행과 라이브러리 탭의 마우스 드래그 스크롤을 지원합니다. 이미지 끌기와 드래그 후 오클릭을 방지합니다.
- 라이브러리 접근 권한과 개별 도서 콘텐츠 권한을 적용한 뒤 신규 작품을 선정합니다.
- 제공처 우선순위 변경과 설정 저장 후 선택 상태가 풀리던 문제를 수정했습니다.

## 2.1.11 - 2026-09-22

### 자동 수집 설정 화면 정리

- 자동 수집 필드에서 작품 소개 항목을 제거했습니다.
- 설정 화면에 작품 소개는 수동 적용에서만 저장된다는 안내를 추가했습니다.

## 2.1.10 - 2026-09-22

### 자동 수집 작품 소개 제외

- 자동 메타데이터 수집에서 외부 제공처의 긴 작품 소개를 자동으로 저장하지 않습니다.
- 작가·출판사·장르·태그·링크·표지 등 나머지 메타데이터는 기존처럼 수집합니다.
- 수동 메타데이터 검색 결과를 직접 적용할 때는 작품 소개를 계속 사용할 수 있습니다.

## 2.1.9 - 2026-09-22

### 미디어 유형별 폴더 작가명 매칭

- 만화·소설·웹툰 폴더도 `시리즈명 [작가명]` 형식을 자동 메타데이터 검색에 사용합니다.
- 작가명이 일치하는 외부 후보를 우선 선택하고, 다른 작가의 동일 제목은 자동 적용하지 않습니다.
- `1080x`, `1920x1080`, 상태·출처 태그, Kyobo S/E 코드는 작가명으로 처리하지 않습니다.

## 2.1.8 - 2026-09-22

### 교보 S/E 코드 자동 조회

- 파일명이나 수동 검색어에 `S`·`E` 12자리 코드가 있으면 교보 상품 페이지를 직접 조회합니다.
- 리디는 코드가 제거된 작품명으로 계속 검색하고, 교보 결과와 리디 결과를 함께 병합합니다.
- 자동 수집에서도 시리즈 권 파일명에서 코드를 찾아 교보 직접 조회를 사용합니다.
- `S` 코드는 일반 교보 상품 URL, `E` 코드는 교보 eBook URL로 연결합니다.

## 2.1.7 - 2026-09-22

### 도서 폴더 작가명 자동 매칭

- 도서 라이브러리의 `시리즈명 [작가명]` 폴더 규칙을 자동 메타데이터 수집에 반영합니다.
- 외부 검색은 대괄호 앞의 시리즈명으로 수행하고, 검색 결과의 작가명이 폴더 작가명과 일치하는 후보를 우선 선택합니다.
- 작가명이 확인된 동일 제목 후보가 모두 다른 작가인 경우 자동 적용하지 않습니다.
- 만화·라노벨·웹툰 라이브러리는 기존 대괄호 처리와 매칭 규칙을 유지합니다.

## 2.1.6 - 2026-09-22

### 리디 수동 소설 검색 안정화

- 수동 메타데이터 검색은 제공처별 결과를 캐시하지 않아, 리디가 일시적으로 응답하지 않은 뒤 네이버 결과만 고정되는 문제를 막습니다.
- `소설`·`라노벨` 같은 표시 이름과 구버전 코어의 자료 유형을 표준 유형으로 정규화합니다.
- `unspecified` 라이브러리에서도 EPUB·MOBI·AZW·TXT 같은 전자책 파일은 소설 검색 탭을 선택할 수 있게 했습니다.
- 검색 요청에 실제 자료 유형·사용한 제공처를 로그로 남겨 NAS에서 리디 연결 여부를 확인할 수 있습니다.

## 2.1.5 - 2026-09-22

### 검색 캐시 갱신

- 리디 소설 검색 수정 결과가 이전 네이버 전용 캐시와 섞이지 않도록 검색 캐시 버전을 갱신했습니다.

## 2.1.4 - 2026-09-22

### 리디 소설 검색 탭 수정

- 리디의 현재 검색 구조에 맞춰 웹소설·라이트노벨·도서 소설 탭을 순서대로 조회합니다.
- 비어 있는 `NOVEL` 집계 탭 때문에 소설 검색에서 리디 결과가 누락되던 문제를 수정했습니다.
- 혼합 응답에 포함된 만화 e북을 소설 검색 결과에서 제외하고, 구조화된 리디 결과와 탐색 링크가 중복 표시되지 않도록 정리했습니다.

## 2.1.3 - 2026-09-22

### 완독 도서 컨텍스트 메뉴

- 플러그인 상세 화면의 권 카드가 도서 메뉴를 열 때 읽음 진행 상태를 함께 전달하도록 수정했습니다.
- 이미 완독했거나 진행률이 있는 도서는 `읽지 않은 상태로 변경`으로 표시되고, 미독 도서만 `읽은 상태로 변경 (완독)`으로 표시됩니다.

## 2.1.2 - 2026-09-22

### 리디 소설 검색과 수동 검색어

- 리디 소설 검색을 라이트노벨 전용 탭에서 전체 소설 탭으로 변경해 웹소설과 일반 소설도 검색하도록 수정했습니다.
- 리디 검색 결과의 자료 유형을 라이트노벨로 고정하지 않고 웹소설·라이트노벨·소설로 구분해 표시합니다.
- 수동 메타데이터 검색에서는 `회귀한 권왕은`처럼 짧은 제목으로 검색해도 더 긴 정식 제목 후보를 보여줍니다.
- 도서 유형을 전달하지 않는 공통 수동 검색에서는 리디 전체 탭을 사용해 만화 검색으로 잘못 제한하지 않습니다.
- 자동 메타데이터 수집은 기존처럼 제목이 정확히 일치하는 결과만 적용하고, 수동·자동 검색 캐시를 분리했습니다.

## 2.1.1 - 2026-09-22

### 모바일 액션 버튼

- 좁은 화면에서 읽기 버튼의 아이콘과 `시리즈 읽기` 문구가 두 줄로 나뉘지 않도록 고정했습니다.
- 280px 이하 화면에서는 읽기·즐겨찾기·컬렉션 버튼의 폭과 간격을 조정해 액션 행이 넘치지 않게 했습니다.

## 2.1.0 - 2026-09-22

### 상세 화면 파일 동작과 로딩 안정성

- 시리즈 둘러보기 카드에서 표지 영역만 읽기를 시작하도록 제한하고, 제목·상태·진행률을 눌렀을 때 잘못 재생되지 않게 했습니다.
- EPUB·PDF·TXT 파일 형식은 상세정보와 시리즈 카드에서 다운로드 링크로 표시하고, 다운로드가 시작되면 하단 알림을 표시합니다. CBZ·ZIP은 다운로드 대상이 아니므로 동작하지 않습니다.
- 모바일 평점의 별점과 인원·평균 표시를 같은 줄에 배치하고, 좁은 화면에서도 액션 버튼과 진행률이 밀리지 않도록 정리했습니다.
- 시리즈 카드의 제목과 진행률 영역 높이를 고정해 긴 제목도 카드 레이아웃이 흔들리지 않게 했습니다.
- 서버가 메타데이터 수집 완료를 알린 뒤에는 비어 있을 수 있는 선택 필드 때문에 상세정보와 파일 목록을 반복 조회하지 않게 했습니다.
- 메타데이터 수집 중 표지·소개만 바뀐 경우 관련작품과 추천항목을 다시 조회하지 않고, 추천 결과가 실제로 바뀔 때만 화면을 갱신해 깜빡임을 줄였습니다.

## 2.0.1 - 2026-09-21

### 릴리스 문서 및 자동 수집 안내

- 플러그인 버전 표기와 `VERSION`, README를 `2.0.1`로 통일했습니다.
- 스캔 후 자동 수집은 BookOasis 코어가 신규 도서 스캔 후크를 호출하는 환경에서 실행되며, 기존 자료만 다시 스캔할 때는 코어의 스캔 완료 후크 지원이 필요하다는 조건을 문서에 명시했습니다.

## 2.0.0 - 2026-09-21

### 상세 화면

- 모바일 상세정보 패널에서 메타정보 수정 또는 메타데이터 검색을 누르면 정보 패널을 자동으로 닫도록 수정했습니다.
- 모바일 상세 화면을 배너, 제목·원제, 표지, 작가 정보, 읽기·즐겨찾기·컬렉션, 독서 진행률, 평점, 태그, 링크, 시리즈, 작품 소개, 관련작품, 추천항목 순서로 정리했습니다.
- 모바일 정보 버튼을 누르면 상세 메타데이터를 한 번에 확인할 수 있는 패널을 표시하고, 수정·검색 버튼을 패널 안에 배치했습니다.
- 배너 색상 영역과 상세 화면 외곽 여백을 자연스럽게 연결하고, 작은 화면에서 검색·수정 화면이 겹치지 않도록 정리했습니다.
- 제목·원제·작품 소개에서 제공처 표기, 작품소개 문구, 불필요한 파일명 접미사를 정리했습니다.
- 시리즈 둘러보기와 다음 이야기에서 시리즈명이 아닌 파일명 기반 권·화 제목을 표시하고, 권·화·Volume/Number 조합에 맞춰 카드와 읽기 동작을 구분했습니다.
- Series.db 관계 유형과 라이브러리 자료 유형을 비교해 관련작품을 연결하고, 관련작품과 추천항목의 중복 및 로딩 깜빡임을 줄였습니다.

### 메타데이터 검색·수집

- 스캔 후 자동 수집을 중첩 데몬 스레드에 맡기지 않고 스캔 후크에서 완료까지 기다리도록 수정했습니다. 신규 도서 후크가 컨테이너 종료나 백그라운드 스레드 정리로 조용히 사라지지 않으며, 신규 도서와 재스캔 후크가 동시에 실행되어도 한 번에 하나씩 처리합니다.
- 신규 도서가 포함된 스캔에서도 완료 후크가 자동 수집을 다시 확인하도록 수정했습니다. 신규 도서 후크가 지연되거나 실패해도 완료 후 수집을 건너뛰지 않습니다.
- 자동 수집 대상 권수·제공처·필드·검색어를 로그에 남기고, 제공처 네트워크 오류와 상세 페이지 수집 실패를 원인과 함께 기록하도록 수정했습니다.
- 일시적인 제공처 오류로 빈 검색 결과가 5분 동안 캐시되지 않도록 검색 캐시 버전을 갱신했습니다. 다음 스캔에서 바로 재시도할 수 있습니다.
- Series.db, 리디, 네이버, 교보문고 제공처의 검색 우선순위와 선택 상태를 저장하고 순서대로 검색하도록 정리했습니다.
- 검색어에서 제거할 키워드와 유지할 키워드를 분리하고, 대괄호·해상도·제공처 접미사를 검색 전에 정리했습니다.
- 검색 결과 카드에 자료 유형(만화 e북, 만화 연재, 웹툰, 라이트노벨 등), 표지, 작품명, 작가·출판사, 메타데이터 적용 버튼을 고정된 레이아웃으로 표시했습니다.
- 리디·네이버 검색 결과의 표지와 원제·관련 링크를 보완하고, 링크는 제공처 파비콘으로 표시했습니다.
- 자동 수집에서 Series.db는 원제·표지·링크 중심으로 사용하고 외부 제공처의 필드와 중복 링크가 충돌하지 않도록 처리했습니다.
- 장르·출판사 변환 규칙, 제외 장르·제외 태그, 자동으로 가져올 필드, 표지 및 기존 값 덮어쓰기 설정을 추가·정리했습니다.
- 표지·덮어쓰기 자료 유형 선택지를 DB의 등록된 속성만 사용하도록 수정해, 설치 환경에 없는 웹툰 등의 항목이 임의로 표시되지 않게 했습니다.
- 작품 소개에 붙던 `작품소개:` 텍스트와 잘못 병합된 본문·태그 값을 정리했습니다.

### 평점 및 안정성

- 커뮤니티 평점 별점의 hover, focus, 반점수 선택, 저장 동작을 보완했습니다.
- 평점 조회가 일시적으로 실패해도 일반도서 별표가 정적 텍스트로 고정되지 않도록 하고, 저장 실패 원인을 화면 알림으로 표시합니다.
- 메타데이터 적용 뒤 표지 캐시를 갱신하고, 상세 화면을 다시 열지 않아도 변경된 값을 반영하도록 정리했습니다.
- 상세 화면의 비동기 요청 순서를 확인해 늦게 도착한 이전 응답이 현재 화면을 덮어쓰지 않도록 보완했습니다.
- Git 저장소 설치 검증에 필요한 `update_manifest`를 추가해 플러그인 매니저의 자동 업데이트 계약을 선언했습니다.
- 홈 위젯의 `initial_data`를 다시 활성화해 첫 화면 HTML에 초기 데이터를 주입하고, 홈 위젯 로딩 깜빡임과 첫 API 요청을 줄였습니다.
